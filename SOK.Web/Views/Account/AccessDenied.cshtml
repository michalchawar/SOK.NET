@{
    ViewData["Title"] = "Brak dostępu";
}

<div class="min-h-screen flex items-center justify-center px-4">
    <div class="max-w-2xl w-full text-center">
        <div class="mb-8">
            <div class="inline-flex items-center justify-center w-24 h-24 bg-error/10 rounded-full mb-6">
                <i class="bi bi-shield-x text-6xl text-error"></i>
            </div>
            
            <h1 class="text-4xl font-bold mb-4">
                Brak dostępu
            </h1>
            
            <p class="text-lg opacity-70 mb-8">
                Niestety, nie masz wystarczających uprawnień, aby zobaczyć tę stronę.
            </p>
            
            <div class="alert alert-warning shadow-lg mb-8">
                <i class="bi bi-exclamation-triangle text-xl"></i>
                <div class="text-left">
                    <h3 class="font-semibold">Dlaczego to widzę?</h3>
                    <p class="text-sm">
                        Ta strona wymaga specjalnych uprawnień. Skontaktuj się z administratorem systemu,
                        jeśli uważasz, że powinieneś mieć do niej dostęp.
                    </p>
                </div>
            </div>
        </div>
        
        <div class="flex flex-col sm:flex-row gap-3 justify-center">
            <a asp-controller="Home" asp-action="Index" class="btn btn-primary">
                <i class="bi bi-house-fill"></i>
                Strona główna
            </a>
            <a asp-controller="Account" asp-action="Logout" class="btn btn-ghost">
                <i class="bi bi-box-arrow-right"></i>
                Wyloguj się
            </a>
        </div>
    </div>
</div>
